version: '3.8'

services:
  bcb-simulator:
    build: ./bcb-simulator
    container_name: bcb-simulator
    environment:
      - BCB_BASE_RATE=6.96
      - BCB_MAX_DELTA=0.9999
      - BCB_REFRESH_SECONDS=180
    ports:
      - "8000:8000"

